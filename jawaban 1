class Recursive():
    def factorial(self, number):
        return 1 if (number==1 or number==0) else number * self.factorial(number - 1);


recursive = Recursive()
factorial = recursive.factorial(10)
print(factorial)
